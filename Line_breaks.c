#include<stdio.h>
#include<string.h>
void solve(){
    int n,m;
    scanf("%d %d",&n,&m);
    int count=0;
    int current_len=0;
    int stop=0;

    printf("%d\n",count);
}
int main(){
    int t;
    if(scanf("%d",&t)==1){
        while(t--)
            solve();
    }
    return 0;
}
