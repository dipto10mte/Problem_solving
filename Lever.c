#include<stdio.h>
int main(){
    int t;
    scanf("%d",&t);
    while(t--){
        int n;
        scanf("%d",&n);
        long long a[200005],b[200005];
        for (int i=0;i<n;i++)
            scanf("%lld",&a[i]);
        for (int i=0;i<n;i++)
        scanf("%lld",&b[i]);

    }
    return 0;
}
