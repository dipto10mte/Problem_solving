#include <stdio.h>
int main(void) {
    int t;
    scanf("%d",&t);
    while(t--) {
        int n;
        scanf("%d",&n);
        char ans[4];

        found:
        printf("%s\n", ans);
    }
    return 0;
}
