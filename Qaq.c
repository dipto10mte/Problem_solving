#include <stdio.h>
#include <string.h>
int main(){
    char s[105];
    scanf("%s",s);
    int n=strlen(s);
    long long ans=0;
    for(int i=0;i<n;i++){
        if(s[i]=='A') {
            long long left=0,right=0;

    }
    printf("%lld\n",ans);
    return 0;
}
