#include<stdio.h>
#include<stdlib.h>
int main(){
    int n;
    if(scanf("%d",&n)!=1)
        return 1;
    int a[105];
    for(int i=0;i<n;i++)
        scanf("%d",&a[i]);

    printf("%d %d\n",idx1,idx2);
    return 0;
}
