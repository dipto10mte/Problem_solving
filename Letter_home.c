#include <stdio.h>
#include <stdlib.h>
int abs_val(int x){
    return x < 0 ? -x : x;
}
int min_val(int a, int b){
    return a < b ? a : b;
}
int main(){
    int t;
    scanf("%d", &t);
    while (t--){
        int n,s;
        scanf("%d %d",&n,&s);
        int *x=malloc(n*sizeof(int));

    }
    return 0;
}
