#include <stdio.h>
int main(){
    int t;
    scanf("%d", &t);
    while (t--) {
        int n;
        scanf("%d",&n);
        int a[105];
        int seen[55]={0};
        for(int i=0;i<2*n;i++)
            scanf("%d", &a[i]);
        int p[55],idx=0;

    }
    return 0;
}
